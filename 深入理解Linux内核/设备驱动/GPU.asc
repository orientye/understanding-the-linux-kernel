:toc:
:toclevels: 5
:hardbreaks-option:

== GPU
=== 概念
==== 显示、渲染与通用计算
GPU(Graphics Processing Unit)最初面向图形加速，在 Linux 上通常同时承担三类工作：

- 显示(Display / Scanout)

    将帧缓冲(framebuffer)按时序送到面板或显示器。
    涉及模式设置(mode setting)：分辨率、刷新率、色深、连接器(connector)与 CRTCs 等。
    在内核中主要由 DRM 的 KMS 路径负责；对应设备节点常为 primary node(/dev/dri/cardN)。

- 渲染(Render / 3D)

    执行绘制、合成等图形命令，产出图像到 buffer，再由显示路径 scanout，或交给合成器(compositor)。
    现代栈里用户态(Mesa 等)提交命令，内核 DRM 负责缓冲、地址空间、调度与同步。
    无显示权限的渲染可走 render node(/dev/dri/renderDN)，便于沙箱与多用户共享 GPU。

- 通用计算(GPGPU / Compute)

    把 GPU 当作并行计算设备：科学计算、机器学习、媒体编解码加速等。
    常见用户态 API：OpenCL、CUDA、Vulkan Compute；底层仍可能通过 DRM 驱动、厂商专有接口或专用计算栈与硬件交互。
    与“显示/渲染”共享同一块 GPU 时，内核侧仍要处理显存、调度与同步，但不必经过 KMS 显示管线。

==== 内核驱动与用户态驱动
现代 GPU 栈通常把驱动拆成内核态与用户态两层，职责不同、演进节奏也不同：

- 内核驱动(Kernel Driver)

    运行在内核，挂在 DRM 等子系统上，直接面对硬件。
    负责：显存与缓冲对象、命令队列与调度、中断、电源管理、模式设置(KMS)、跨进程隔离与安全边界。
    通过 /dev/dri/cardN、/dev/dri/renderDN 等节点向用户态提供 ioctl；典型实现如 i915、amdgpu、nouveau。

- 用户态驱动(Userspace Driver)

    运行在进程内，常见于 Mesa（OpenGL/Vulkan）或厂商专有用户态栈。
    负责：着色器编译、命令缓冲构建、API 实现，经 libdrm 等与内核驱动交互，把工作提交给 GPU。
    例：Mesa 的 iris / radeonsi / nouveau；NVIDIA 专有栈的用户态部分。

- 为何分层

    硬件指令集与图形 API 变化快，放在用户态可独立升级，不必频繁改内核 ABI。
    内核保留特权路径：仲裁多进程共享、校验 DMA/地址空间、保证安全与稳定性。
    与「开源 / 专有」正交：开源或专有栈都可以是「内核驱动 + 用户态驱动」的组合。

==== 开源驱动与专有驱动
- GPU 驱动程序

    开源驱动:
        许多 GPU 制造商（如 NVIDIA、AMD 和 Intel）提供开源驱动程序
        如 Intel 的 i915、AMD 的 amdgpu 和 Nouveau（NVIDIA 的开源驱动）。
    专有驱动:
        一些公司（如 NVIDIA）也提供专有驱动程序，提供更好的性能和更多功能，但可能不如开源驱动灵活
        如 NVIDIA 的官方驱动（nvidia）和 AMD 的 amdgpu-pro。

- DRM 子系统

    Direct Rendering Manager (DRM):
        内核中的一个子系统，负责管理 GPU 的资源和调度。
        DRM 提供了对 GPU 的直接访问，允许应用程序直接与 GPU 进行交互，而不需要通过 X 服务器。
    KMS (Kernel Mode Setting):
        KMS 是 DRM 的一部分，允许内核控制显示模式和分辨率，提供更平滑的图形体验。

=== 演进
==== DRM 的形成与职责

==== KMS 与内核模式设置

==== 用户态图形栈的演进

=== 硬件与设备发现
==== GPU 硬件组成

==== PCI、platform 与设备树/ACPI
- 设备树和硬件支持

    设备树:
        在某些情况下，特别是在嵌入式系统中，GPU 的信息可能通过设备树传递给内核，以便正确识别和配置硬件。

==== 驱动加载与初始化

=== 内核子系统
==== DRM 核心框架

==== KMS：显示管线

==== 内存管理：GEM、TTM 与显存对象

==== 命令提交与 GPU 调度

==== 同步与缓冲区共享：fence、dma-buf、syncobj

==== 地址空间：GPUVA 与进程内存

==== DMA 与 IOMMU

==== 电源管理与运行时 PM

==== 可观测性与调试：debugfs、tracepoint、drm_info

==== 厂商驱动实例：i915、amdgpu、nouveau

=== 用户态图形栈
==== 设备节点与访问控制：primary、render 节点

==== libdrm
- 相关工具和库

    libdrm:
        用户空间库，提供对 DRM 的访问，允许应用程序与内核中的 GPU 驱动程序进行交互。

==== Mesa、OpenGL、Vulkan

    Mesa:
        提供对 OpenGL 和 Vulkan 的支持，允许在 Linux 上使用 GPU 进行图形渲染。

==== Wayland/X 与合成器

=== GPU 计算
==== 计算 API：OpenCL、CUDA、Vulkan Compute
- GPU 计算

    CUDA 和 OpenCL:
        对于需要 GPU 计算的应用程序，NVIDIA 提供 CUDA，而其它 GPU 制造商（如 AMD）则支持 OpenCL。

==== 用户态运行时与内核驱动的边界

=== 源码
https://github.com/torvalds/linux/tree/master/drivers/gpu

=== 案例
==== Asahi Linux M1 GPU
https://asahilinux.org/2022/11/tales-of-the-m1-gpu/

=== 参考
https://www.kernel.org/doc/html/latest/gpu/
